# 赵元祯 — 个人网站

一个中英双语静态个人网站，围绕物理、统计、生成模型、科学计算与金融市场中的跨领域问题展开。

## 本地开发

```bash
npm install
npm run dev
```

## 验证

```bash
npm test
npm run validate:content
npm run typecheck
npm run lint
npm run build
```

静态导出位于 `out/`；英文页面为 `/`，中文页面为 `/zh/`。
