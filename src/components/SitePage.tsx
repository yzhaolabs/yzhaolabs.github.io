import Archive from "./Archive"; import Contact from "./Contact"; import FeaturedExplorations from "./FeaturedExplorations"; import Footer from "./Footer"; import Hero from "./Hero"; import Navbar from "./Navbar"; import Path from "./Path"; import QuestionMap from "./QuestionMap";
import { getContent } from "@/content/get-content"; import type { Locale } from "@/content/types";
export default function SitePage({ locale }: { locale: Locale }) { const content = getContent(locale); return <><Navbar locale={locale} content={content.nav}/><main><Hero locale={locale} content={content.hero}/><QuestionMap content={content.questions} section={content.sections.questions}/><FeaturedExplorations content={content}/><Archive locale={locale} content={content}/><Path content={content.path} section={content.sections.path}/><Contact content={content.contact} label={content.sections.contact.label}/></main><Footer name={content.footer} locale={locale} label={content.nav.language}/></>; }
